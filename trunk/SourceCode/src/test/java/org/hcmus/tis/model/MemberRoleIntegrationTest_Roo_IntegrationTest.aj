// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.hcmus.tis.model;

import java.util.List;
import org.hcmus.tis.model.MemberRole;
import org.hcmus.tis.model.MemberRoleDataOnDemand;
import org.hcmus.tis.model.MemberRoleIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect MemberRoleIntegrationTest_Roo_IntegrationTest {
    
    declare @type: MemberRoleIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: MemberRoleIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: MemberRoleIntegrationTest: @Transactional;
    
    @Autowired
    private MemberRoleDataOnDemand MemberRoleIntegrationTest.dod;
    
    @Test
    public void MemberRoleIntegrationTest.testCountMemberRoles() {
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to initialize correctly", dod.getRandomMemberRole());
        long count = MemberRole.countMemberRoles();
        Assert.assertTrue("Counter for 'MemberRole' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void MemberRoleIntegrationTest.testFindMemberRole() {
        MemberRole obj = dod.getRandomMemberRole();
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to provide an identifier", id);
        obj = MemberRole.findMemberRole(id);
        Assert.assertNotNull("Find method for 'MemberRole' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'MemberRole' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void MemberRoleIntegrationTest.testFindAllMemberRoles() {
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to initialize correctly", dod.getRandomMemberRole());
        long count = MemberRole.countMemberRoles();
        Assert.assertTrue("Too expensive to perform a find all test for 'MemberRole', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<MemberRole> result = MemberRole.findAllMemberRoles();
        Assert.assertNotNull("Find all method for 'MemberRole' illegally returned null", result);
        Assert.assertTrue("Find all method for 'MemberRole' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void MemberRoleIntegrationTest.testFindMemberRoleEntries() {
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to initialize correctly", dod.getRandomMemberRole());
        long count = MemberRole.countMemberRoles();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<MemberRole> result = MemberRole.findMemberRoleEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'MemberRole' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'MemberRole' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void MemberRoleIntegrationTest.testFlush() {
        MemberRole obj = dod.getRandomMemberRole();
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to provide an identifier", id);
        obj = MemberRole.findMemberRole(id);
        Assert.assertNotNull("Find method for 'MemberRole' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyMemberRole(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'MemberRole' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MemberRoleIntegrationTest.testMergeUpdate() {
        MemberRole obj = dod.getRandomMemberRole();
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to provide an identifier", id);
        obj = MemberRole.findMemberRole(id);
        boolean modified =  dod.modifyMemberRole(obj);
        Integer currentVersion = obj.getVersion();
        MemberRole merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'MemberRole' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MemberRoleIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to initialize correctly", dod.getRandomMemberRole());
        MemberRole obj = dod.getNewTransientMemberRole(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'MemberRole' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'MemberRole' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void MemberRoleIntegrationTest.testRemove() {
        MemberRole obj = dod.getRandomMemberRole();
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MemberRole' failed to provide an identifier", id);
        obj = MemberRole.findMemberRole(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'MemberRole' with identifier '" + id + "'", MemberRole.findMemberRole(id));
    }
    
}
